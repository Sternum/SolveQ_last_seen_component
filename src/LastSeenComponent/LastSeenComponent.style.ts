import { StyleSheet } from "react-native";

const LastSeenComponentStyle = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center'
    },
    label: {
        fontSize: 18
    },
    timeLabel: {
        fontSize: 14
    }
});

export default LastSeenComponentStyle;
